# Quiz Palの構成と工夫

問題を解く画面から、その問題について質問するところまで続けられる学習ページです。基本の学習と手動の教材作成は、APIキーなしで試せます。

## 操作と保存

| 操作 | 主な実装 |
| --- | --- |
| 問題を解く、進捗・理解度を記録する | `index.html`、`public/app.js` |
| 教材を追加・編集する | `src/App.tsx`、`src/features/` |
| クイズと編集画面で同じ教材を使う | `src/bridge/legacyDataset.ts`、`src/db/` |
| 画像を保存し、解説画像ボタンから見る | `public/app.js`の画像一覧・IndexedDB設定保存 |
| 全データをセーブ／ロードする | クイズ側の完全バックアップと、教材管理側のバックアップ |
| CSVを検査して取り込む | `src/features/csv-import/` |
| AI設定・直接通信 | `public/llm-provider-bridge.js` |
| 初回の紹介と操作ガイド | `public/product-intro.js`、`public/quiz-tour.js` |
| サーバー不要のHTML版 | `portable/html/`、`scripts/build-portable-html.mjs` |

HTML共通版は入口を`index.html`へ統一し、クイズ・編集・利用条件を同じファイルの画面切替で開きます。ファイルごとに保存環境が分かれる問題を避け、セーブ／ロードで別環境へ移せるようにしています。従来版の保存キーを維持しているため、コード内の一部に旧プロジェクト名が残っています。

## ブラウザー標準機能による実装

DOMとCanvas 2Dで画面を作り、IndexedDBとlocalStorageで保存します。UIレンダラーは`src/native-ui/`、入力検証は`src/native-validation.ts`、DBラッパーは`src/native-db.ts`です。外部ライブラリの互換名と、ライブラリ本体の使用を区別しています。

紹介画像を表示サイズへ縮小し、日本語フォントを通常文字と追加文字へ分割して、初回の取得量を減らしました。フォントの元の全文字は保持しています。狭い画面ではページ全体を横へ広げず、必要なボタン列だけをスクロールできます。

## 参考元と制作の説明

[hinahina-vr/exam-prep-quiz](https://github.com/hinahina-vr/exam-prep-quiz)を参考に、教材管理画面、保存・復元、ローカルHTML版、初回の紹介とガイドなどを含む構成にしています。参考元や既存コードの出典を隠さず、本人の設計・実装・検証の担当範囲は応募説明へ追記してください。

AIによる制作支援を使っています。利用者が作品内で使う任意のAI機能とは区別し、制作期間・本人の担当・工夫した点を応募者本人の言葉で補足します。

## 紹介と読みやすさ

本文はM PLUS Rounded 1cのMedium、見出しはBoldを使用し、薄い文字色も濃くしました。前の会話を踏まえて聞き直せること、外部の図解を保存して見返せることを中心に紹介します。会話は実DeepSeek V4 Flashの応答を3枚の画像で切替・拡大でき、キー不要で流れを確認できます。紹介を閉じても自動的に非表示設定にはせず、「もう表示しない」の明示選択だけを記憶します。操作ガイドは画像保存を加えた9段階です。

## AIを選ぶ画面と接続手順

AIの選択は名前と一文だけの2列一覧にし、料金と接続手順は選択後の専用ページへ移しました。カード上部の色帯と囲みを取り除き、細い罫線・文字の大小・余白で順序を示します。実会話と図解保存も別表示にし、大きい画面で縦に長くならない構成にしました。スクロールバーを隠さず、必要な情報が収まるかを回帰テストで測定します。小さい画面では縦スクロールを維持します。
