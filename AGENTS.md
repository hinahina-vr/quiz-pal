# このリポジトリでの作業

2026年9月12日、ユーザーの明示指示によりコンテスト提出用のPublicリポジトリへ公開します。公開・確認の完了後は凍結し、致命的な不具合の修正以外は行いません。機能追加、デザイン変更、リファクタリング、通常の依存更新、文言の調整は対象外です。応募フォームの送信は依頼されていません。

凍結後の例外は、起動不能、主要学習・保存／復元の破綻、データ消失、重大なセキュリティ問題などに限ります。修正前に再現し、最小限の変更と回帰確認を行い、原因・影響・修正版・検証結果を記録してください。審査タグ`v1.0.0`は移動・削除せず、致命的不具合の修正版には別のタグを付けます。

- 編集対象は`src/`、`public/`、`portable/html/`と入口HTMLです。`docs/`は`npm run build`で再生成します。
- バージョンアップ時は、変更機能と関連する既存機能、保存・書出し・導入先までの利用手順を実際にテストします。
- 不具合修正は症状を再現し、同じ操作・条件での回帰テストを残します。画面や連携はファイルの存在だけで確認済みにしません。
- 最終ZIPは新しいフォルダーへ展開して確認します。検証後に変更した場合は影響する操作を再確認し、成果物のハッシュを更新します。
- 合格した試験と未確認範囲をQA.mdへ記録します。モック・端末エミュレーション・ローカルHTTPを実アプリの外部サービス接続、実機スマートフォン、公開サイトでの確認と表現しません。
- APIキー、個人のセーブ、ブラウザープロファイル、作業ログを公開対象へ入れません。`tests/fixtures/`は合成試験データです。
- GitHub Pagesでの実際の公開後は、公開ファイルと審査コミットの一致、PC Chrome、実機スマートフォンChromeを別途確認します。

詳しい手順はTESTING.mdとPUBLISHING.mdを参照してください。
